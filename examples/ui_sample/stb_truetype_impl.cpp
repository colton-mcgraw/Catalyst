/*
 * @file stb_truetype_impl.cpp
 * @brief The one translation unit that stamps out stb_truetype's implementation for the UI sample.
 * @details Third-party code compiled verbatim at the commit cmake/stb.cmake pins; its warnings are silenced
 * per-source in this directory's CMakeLists.txt rather than patched. Only built when the resource module fetched stb.
 * License: MIT (see LICENSE).
 */

#define STB_TRUETYPE_IMPLEMENTATION
#include <stb_truetype.h>
