# Left out of `git archive` and the tarballs GitHub builds from a tag: they are for contributors
# working in a clone, not for a consumer unpacking a release.
.github/         export-ignore
.clang-format    export-ignore
.gitattributes   export-ignore
.gitignore       export-ignore
